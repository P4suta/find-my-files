# Rust
engine/target/
xtask/target/

# .NET / WinUI
app/**/bin/
app/**/obj/
.vs/
*.user

# Build artifacts & distribution
dist/
# Release assets produced by `just package` (xtask)
find-my-files-*-win-x64.zip
SHA256SUMS.txt
# SBOMs generated at release time (release.yml), uploaded as release assets
*.cdx.json
sbom-app/
screenshots/
test-results.json
**/*.cobertura.xml

# Generated docs (mdBook output; rustdoc lives under engine/target/)
docs/book/
# Docs staged into the site by `just docs-assemble` (pages.yml does the same)
site/book/
site/doc/

# Engine runtime artifacts
*.fmfidx
engine/crates/fmf-core/testdata/large/

# Local-only tool overrides
mise.local.toml
.mise.local.toml

# Claude Code local working dir (scheduled task locks, etc.)
.claude/

# Internal working notes — never publish
CLAUDE.md

# Untracked screenshot scratch
.tmp-shots/
