<?xml version="1.0" encoding="utf-8" ?>
<!--
    Self-built Fluent settings surface (no CommunityToolkit dependency — the
    toolkit's SettingsControls pulls the WindowsAppSDK 1.x metapackage and
    hard-conflicts with this app's slim 2.x component packages). SettingsCard
    (Controls/SettingsCard.cs) reuses the same card chrome as the diagnostics
    panel's DiagCardStyle, so settings and diagnostics read as one design
    system. All dimensions come from the token system; colours stay on Fluent
    ThemeResource brushes.
-->
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctl="using:FindMyFiles.Controls">

    <!--  Explicit style key (not implicit / DefaultStyleKey): applied per card
          with Style="{StaticResource SettingsCardStyle}" so the template resolves
          without a Themes/Generic.xaml convention.  -->
    <Style x:Key="SettingsCardStyle" TargetType="ctl:SettingsCard">
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="{StaticResource CardBorderThickness}" />
        <Setter Property="CornerRadius" Value="{ThemeResource OverlayCornerRadius}" />
        <Setter Property="Padding" Value="{StaticResource CardPadding}" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ctl:SettingsCard">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        Padding="{TemplateBinding Padding}">
                        <Grid ColumnSpacing="{StaticResource SpacingMedium}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="{StaticResource SpacingMedium}">
                                <!--  Optional leading icon — the control collapses it when Glyph is unset.  -->
                                <FontIcon
                                    x:Name="PART_Icon"
                                    VerticalAlignment="Center"
                                    FontSize="{StaticResource IconSizeMedium}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Glyph="{TemplateBinding Glyph}" />
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock
                                        Style="{ThemeResource BodyTextBlockStyle}"
                                        Text="{TemplateBinding Header}"
                                        TextWrapping="Wrap" />
                                    <!--  Optional description — the control collapses it when Description is unset.  -->
                                    <TextBlock
                                        x:Name="PART_Description"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                        Text="{TemplateBinding Description}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </StackPanel>
                            <ContentPresenter
                                Grid.Column="1"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Content="{TemplateBinding Content}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
