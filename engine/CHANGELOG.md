# Changelog

All notable changes to find-my-files are recorded here.

This file is maintained **automatically** by [release-please](https://github.com/googleapis/release-please)
from [Conventional Commits](https://www.conventionalcommits.org/) — do not edit it
by hand. A merged Release PR adds a new section and cuts the matching `vX.Y.Z`
tag. It lives here under `engine/` (not the repo root) because release-please's
rust release-type must own `engine/Cargo.toml` + `engine/Cargo.lock`, and a
package-relative `changelog-path` cannot escape the package directory. See
[ADR-0035](../docs/adr/0035-automated-versioning-with-release-please-and-build-channels.md).
